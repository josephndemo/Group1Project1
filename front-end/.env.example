# Production backend API URL.
# Replace this with your actual Render backend URL if your Render service uses a different hostname.
VITE_API_BASE_URL=https://group1project1-1.onrender.com

# false = use Flask backend for Book Club data.
# true = use local mock Book Club data only.
VITE_USE_MOCK_BOOK_CLUB=false
