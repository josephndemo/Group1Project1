services:
  - type: web
    name: group1project1-backend
    env: python
    rootDir: back-end
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
