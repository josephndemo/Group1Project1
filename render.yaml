services:
  - type: web
    name: group1project1-backend
    env: python
    rootDir: back-end
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    autoDeploy: true
